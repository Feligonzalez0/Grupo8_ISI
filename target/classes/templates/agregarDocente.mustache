<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Registrar Docente</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-xl">

        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Registrar Nuevo Docente</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-300 text-green-800 p-4 rounded mb-4">
            {{successMessage}}
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-300 text-red-800 p-4 rounded mb-4">
            {{errorMessage}}
        </div>
        {{/errorMessage}}

        <form action="/docente/new" method="post" class="space-y-4">

            <div>
                <label for="dni" class="block text-gray-700 font-medium">DNI</label>
                <input 
                    type="text" 
                    id="dni" 
                    name="dni" 
                    required
                    class="w-full border border-gray-300 rounded p-2 focus:ring-2 focus:ring-blue-400"
                >
            </div>

            <div>
                <label for="nombre" class="block text-gray-700 font-medium">Nombre</label>
                <input 
                    type="text" 
                    id="nombre" 
                    name="nombre" 
                    required
                    class="w-full border border-gray-300 rounded p-2 focus:ring-2 focus:ring-blue-400"
                >
            </div>

            <div>
                <label for="apellido" class="block text-gray-700 font-medium">Apellido</label>
                <input 
                    type="text" 
                    id="apellido" 
                    name="apellido" 
                    required
                    class="w-full border border-gray-300 rounded p-2 focus:ring-2 focus:ring-blue-400"
                >
            </div>

            <div>
                <label for="codigo_profesor" class="block text-gray-700 font-medium">Código Profesor</label>
                <input 
                    type="text" 
                    id="codigo_profesor" 
                    name="codigo_profesor" 
                    required
                    class="w-full border border-gray-300 rounded p-2 focus:ring-2 focus:ring-blue-400"
                >
            </div>

            <button 
                type="submit" 
                class="w-full bg-purple-600 text-white py-2 rounded-lg hover:bg-purple-700 transition duration-300 font-semibold"
            >
                Registrar Docente
            </button>

        </form>

        <div class="text-center mt-6">
            <a href="/dashboard" class="text-blue-600 hover:underline font-semibold">
                Cancelar
            </a>
        </div>

    </div>

</body>
</html>
